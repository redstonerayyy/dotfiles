{
    "layer": "top", // Waybar at top layer
    "position": "top", // Waybar position (top|bottom|left|right)
    "height": 30,
    "margin": "0 0 0 0",
    //"width": 1350, // Waybar width
    // Choose the order of the modules idle_inhibitor
    "modules-left": ["custom/launcher", "wlr/workspaces", "custom/media", "tray"],
    "modules-center": ["clock"],
    "modules-right": ["idle_inhibitor", "backlight", "pulseaudio", "temperature", "cpu", "memory", "battery", "user", "network", "custom/power"], 
    
 // Modules configuration
    "custom/launcher": {
    	"format":" ",
    	"on-click": "exec nwg-drawer -c 7 -is 70 -spacing 23",
    	"tooltip": false,
    },

    "wlr/workspaces":{
	"format": "{name}",
    },

     "custom/media": {
        "format": "{icon} {}",
        "return-type": "json",
        "max-length": 40,
        "format-icons": {
            "spotify": "",
        },
        "escape": true,
        "exec": "/home/harry/.config/waybar/mediaplayer.py 2> /dev/null" // Script in resources folder
    },

    "tray": {
    	//"icon-size": 15,
	"spacing": 10
    },

    
    "clock": {
        "tooltip-format": "{:%A %B %d %Y | %H:%M}",
        "format": "  {:%a %d %b  %H:%M}",	//24 hour format
        "format-alt": "  {:%d/%m/%Y  %H:%M:%S}",
        "interval": 1
    },

    "idle_inhibitor": {
	    "format": "{icon}",
	    "format-icons": {
		"activated": "",
		"deactivated": ""
	    }
    },

    "backlight": {
        "format": "{icon} {percent: >3}%",
        "format-icons": ["", ""],
        "on-scroll-down": "light -A 5 && light -G | cut -d'.' -f1 > $SWAYSOCK.wob",
        "on-scroll-up": "light -U 5 && light -G | cut -d'.' -f1 > $SWAYSOCK.wob"
    },

    "pulseaudio": {
        //"scroll-step": 1,
        "format": "{volume}% {icon} {format_source}",
        "format-bluetooth": "{volume}% {icon} {format_source}",
        "format-bluetooth-muted": " {icon} {format_source}",
        "format-muted": " {format_source}",
        "format-source": "{volume}% ",
        "format-source-muted": "",
        "format-icons": {
            "headphone": "",
            "hands-free": "",
            "headset": "",
            "phone": "",
            "portable": "",
            "car": "",
            "default": ["", "", ""]
        },        "on-click": "pavucontrol",
        "on-scroll-up": "pamixer -ui 2 && pamixer --get-volume > $SWAYSOCK.wob",
        "on-scroll-down": "pamixer -ud 2 && pamixer --get-volume > $SWAYSOCK.wob",
	"ignored-sinks": ["Easy Effects Sink"]
    },
    
    "temperature": {
        // "thermal-zone": 2,
        "hwmon-path": "/sys/class/hwmon/hwmon1/temp1_input",
        "critical-threshold": 80,
        // "format-critical": "{temperatureC}°C ",
        "format": "  {temperatureC}°C"
    },

    "cpu": {
        "format": " {usage: >3}%",
        "on-click": "alacritty -e htop"
    },

    "memory": {
        "format": "RAM: {: >3}%",
        "on-click": "alacritty -e htop"
    },
    
    "battery": {
    	"interval" : 3,
        "states": {
            // "good": 95,
            "warning": 30,
            "critical": 15
        },       
        "format": "{icon}   {capacity: >3}% {power}W {time}",
        "format-icons": ["", "", "", "", ""]
    },

    "user": {
	"format": "{work_H}:{work_M}",
	"interval": 60,
	"icon": true
    },

     "network": {
        "format": "⚠  Disabled",
        "format-wifi": "  {essid}",
        "format-ethernet": "  Wired",
        "format-disconnected": "⚠  Disconnected",
        "on-click": "nm-connection-editor"
    },
    

    "custom/power": {
      	"format":"⏻",
       	"on-click": "nwg-bar",
      	"tooltip": false,
    },

    
}